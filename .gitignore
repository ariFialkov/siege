node_modules/
package-lock.json
dist/
# vendored by `npm install` (postinstall); the game falls back to the CDN without it
lib/
# generated by `npm run icons` (the manifest embeds these as data URIs)
assets/icons/
